{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default CrossBridge window capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
